add_executable(EquityOptionProfitCalculator EquityOptionProfitCalculator.cpp)
target_link_libraries(EquityOptionProfitCalculator ql_library ${QL_THREAD_LIBRARIES})
if (QL_INSTALL_EXAMPLES)
    install(TARGETS EquityOptionProfitCalculator RUNTIME DESTINATION ${QL_INSTALL_EXAMPLESDIR})
endif()